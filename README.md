# Silent Angle Viewer

Static viewer for sniper mission simulation replays and balance reports.

Open `index.html` through GitHub Pages to inspect:

- per-turn replay data
- guard sight coverage
- mission result summary
- variant comparison metrics
